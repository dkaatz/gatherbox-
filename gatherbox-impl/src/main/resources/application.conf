#
# Copyright (C) 2016 Lightbend Inc. <http://www.lightbend.com>
#
play.http.secret.key = whatever
play.application.loader = de.beuth.gatherbox.impl.GatherboxLoader

gatherbox.cassandra.keyspace = gatherbox
akka.loglevel=DEBUG


cassandra-journal.keyspace = ${gatherbox.cassandra.keyspace}
cassandra-snapshot-store.keyspace = ${gatherbox.cassandra.keyspace}
lagom.persistence.read-side.cassandra.keyspace = ${gatherbox.cassandra.keyspace}
